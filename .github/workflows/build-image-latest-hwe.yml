name: Latest Images HWE
on:
  merge_group:
  pull_request:
    branches:
      - main
      - testing
    paths-ignore:
      - "**.md"
      - ".github/workflows/*iso*"
      - "build_files/iso/**"
  push:
    branches:
      - main
      - testing
    paths-ignore:
      - "**.md"
      - ".github/workflows/*iso*"
      - "build_files/iso/**"
  schedule:
    - cron: "40 5 * * *" # 05:40 UTC
  workflow_call:
  workflow_dispatch:

permissions:
    contents: read
    packages: write
    id-token: write

jobs:
  build-image-latest:
    name: Build Latest Images
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    strategy:
      fail-fast: false
      matrix:
        brand_name: ["silver-ublue"]
    with:
      image_flavors: '["hwe", "hwe-nvidia-open"]' # "hwe-nvidia",
      brand_name: ${{ matrix.brand_name }}
      stream_name: latest

  # generate-release:
  #   name: Generate Release
  #   needs: [build-image-latest]
  #   secrets: inherit
  #   uses: ./.github/workflows/generate-release.yml
  #   with:
  #     stream_name: '["latest"]'